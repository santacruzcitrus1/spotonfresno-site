import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
  // Custom domain spotonfresno.com — no basePath needed
};

export default nextConfig;
